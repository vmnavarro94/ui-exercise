import React from 'react';

import Button from '../Button/Button';

const OnListingOptions = props => (
    <div className='options'>
        <Button>Select all</Button>
        <Button>Refresh</Button>
        <Button>More</Button>
    </div>
);

export default OnListingOptions;